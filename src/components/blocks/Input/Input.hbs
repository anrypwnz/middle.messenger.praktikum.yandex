<label class="{{labelClass}}" for="{{id}}">{{label}}</label>
<input id="{{id}}"
       class="{{inputClass}}"
       type="{{type}}"
       placeholder="{{placeholder}}"
       name="{{name}}"
       value="{{value}}"
       autocomplete="{{autocomplete}}">